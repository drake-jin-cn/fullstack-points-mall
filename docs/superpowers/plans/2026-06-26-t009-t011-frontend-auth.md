# TASK-AUTH-0006 Frontend Auth Infrastructure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the foundational HTTP layer (Axios + loading/toast), login page, silent token refresh interceptor, logout, and middleware route guard for `points-mall-frontend`.

**Architecture:** Modular Axios interceptor pipeline (`loading → auth → error`) composes onto a shared instance; Zustand stores hold loading count and user info; Next.js `middleware.ts` guards server-side routes by cookie presence; the login page calls the BFF and relies entirely on `Set-Cookie` for token storage.

**Tech Stack:** Next.js 16 · React 19 · TypeScript · Tailwind CSS · shadcn/ui · Sonner · Zustand · Axios · React Hook Form · Zod · Vitest · @testing-library/react

**Spec:** `docs/superpowers/specs/2026-06-26-t009-t011-axios-auth-design.md`  
**Task:** `.tasks/auth/TASK-AUTH-0006.md`

---

## File Map

| File | Role | New/Modify |
|------|------|-----------|
| `next.config.ts` | Remove `output: 'export'` (required for middleware) | Modify |
| `vitest.config.ts` | Vitest + jsdom setup | New |
| `src/middleware.ts` | Edge route guard — cookie presence check | New |
| `src/lib/http/instance.ts` | Axios instance (`withCredentials`, `baseURL`) | New |
| `src/lib/http/types.ts` | `ApiResponse<T>`, `AxiosRequestConfig` extension | New |
| `src/lib/http/interceptors/loading.ts` | Request counter — increment/decrement loading store | New |
| `src/lib/http/interceptors/error.ts` | Envelope unwrap + Sonner toast on error | New |
| `src/lib/http/interceptors/auth.ts` | 401 → refresh queue → replay | New |
| `src/lib/http/index.ts` | Compose interceptors, export `http` singleton | New |
| `src/lib/auth/logout.ts` | `logout()` — POST /auth/logout → clearUser → redirect | New |
| `src/store/useLoadingStore.ts` | Zustand loading counter | New |
| `src/store/useAuthStore.ts` | Zustand user info + sessionStorage persist | New |
| `src/components/ui/LoadingOverlay.tsx` | Full-screen backdrop + spinner | New |
| `src/components/providers/AppProviders.tsx` | Mount `<LoadingOverlay>` + `<Toaster>` | New |
| `src/app/layout.tsx` | Wrap with `<AppProviders>` | Modify |
| `src/app/(auth)/login/page.tsx` | Login form (RHF + Zod, dark gradient UI) | New |
| `.env.example` | `NEXT_PUBLIC_BFF_URL` | New |
| `__tests__/store/useLoadingStore.test.ts` | Unit tests: counter, clamp | New |
| `__tests__/store/useAuthStore.test.ts` | Unit tests: setUser, clearUser, persist | New |
| `__tests__/lib/http/interceptors/loading.test.ts` | Interceptor tests: increment/decrement on req/res/err | New |
| `__tests__/lib/http/interceptors/error.test.ts` | Interceptor tests: unwrap, toast, traceId | New |
| `__tests__/lib/http/interceptors/auth.test.ts` | Interceptor tests: 401 queue, refresh, replay, loop guard | New |
| `__tests__/lib/auth/logout.test.ts` | Unit tests: API call, fail-safe clear | New |
| `__tests__/middleware.test.ts` | Unit tests: cookie guard, public routes | New |

---

## Task 1: Project Setup — Remove Static Export, Install Dependencies, Configure Vitest

**Files:**
- Modify: `points-mall-frontend/next.config.ts`
- Create: `points-mall-frontend/vitest.config.ts`
- Create: `points-mall-frontend/.env.example`
- Modify: `points-mall-frontend/package.json` (via pnpm)

- [ ] **Step 1: Remove `output: 'export'` from next.config.ts**

`next.config.ts` must be:
```ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {}

export default nextConfig
```

- [ ] **Step 2: Install runtime dependencies**

```bash
cd points-mall-frontend
pnpm add axios sonner zustand react-hook-form @hookform/resolvers zod
```

Expected: packages added to `dependencies` in `package.json`.

- [ ] **Step 3: Install Vitest and test utilities**

```bash
pnpm add -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event axios-mock-adapter
```

- [ ] **Step 4: Create `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

- [ ] **Step 5: Create `src/test-setup.ts`**

```ts
import '@testing-library/jest-dom'
```

- [ ] **Step 6: Add test script to `package.json`**

Add to `scripts`:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 7: Initialize shadcn/ui**

```bash
npx shadcn@latest init -d
```

When prompted, choose:
- Style: Default
- Base color: Slate
- CSS variables: yes

This creates `components.json` and updates `tailwind.config.ts` and `globals.css`.

- [ ] **Step 8: Add shadcn components needed for login page**

```bash
npx shadcn@latest add button input label card form
```

- [ ] **Step 9: Create `.env.example`**

```bash
# BFF base URL — required for all API calls
NEXT_PUBLIC_BFF_URL=http://localhost:3001
```

Also create `.env.local` (not committed) with the same content for local dev.

- [ ] **Step 10: Verify setup compiles**

```bash
pnpm build 2>&1 | tail -5
```

Expected: build succeeds (or only warns about missing pages, not errors).

- [ ] **Step 11: Commit**

```bash
git add .
git commit -m "chore(TASK-AUTH-0006): setup vitest, shadcn/ui, install deps, remove static export"
```

---

## Task 2: Zustand Stores — `useLoadingStore` + `useAuthStore`

**Files:**
- Create: `src/store/useLoadingStore.ts`
- Create: `src/store/useAuthStore.ts`
- Create: `src/__tests__/store/useLoadingStore.test.ts`
- Create: `src/__tests__/store/useAuthStore.test.ts`

- [ ] **Step 1: Write failing tests for `useLoadingStore`**

Create `src/__tests__/store/useLoadingStore.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { act } from '@testing-library/react'
import { useLoadingStore } from '@/store/useLoadingStore'

beforeEach(() => {
  useLoadingStore.setState({ count: 0 })
})

describe('useLoadingStore', () => {
  it('starts with count 0 and isLoading false', () => {
    expect(useLoadingStore.getState().count).toBe(0)
    expect(useLoadingStore.getState().isLoading).toBe(false)
  })

  it('increment increases count and sets isLoading true', () => {
    act(() => useLoadingStore.getState().increment())
    expect(useLoadingStore.getState().count).toBe(1)
    expect(useLoadingStore.getState().isLoading).toBe(true)
  })

  it('decrement decreases count', () => {
    act(() => {
      useLoadingStore.getState().increment()
      useLoadingStore.getState().increment()
      useLoadingStore.getState().decrement()
    })
    expect(useLoadingStore.getState().count).toBe(1)
  })

  it('decrement clamps to 0, never goes negative', () => {
    act(() => useLoadingStore.getState().decrement())
    expect(useLoadingStore.getState().count).toBe(0)
    expect(useLoadingStore.getState().isLoading).toBe(false)
  })

  it('isLoading is false when count reaches 0 after concurrent requests', () => {
    act(() => {
      useLoadingStore.getState().increment()
      useLoadingStore.getState().increment()
      useLoadingStore.getState().decrement()
      useLoadingStore.getState().decrement()
    })
    expect(useLoadingStore.getState().isLoading).toBe(false)
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
cd points-mall-frontend
pnpm test src/__tests__/store/useLoadingStore.test.ts 2>&1 | tail -10
```

Expected: FAIL — "Cannot find module '@/store/useLoadingStore'"

- [ ] **Step 3: Implement `useLoadingStore`**

Create `src/store/useLoadingStore.ts`:
```ts
import { create } from 'zustand'

interface LoadingState {
  count: number
  isLoading: boolean
  increment: () => void
  decrement: () => void
}

export const useLoadingStore = create<LoadingState>((set) => ({
  count: 0,
  isLoading: false,
  increment: () =>
    set((state) => {
      const count = state.count + 1
      return { count, isLoading: count > 0 }
    }),
  decrement: () =>
    set((state) => {
      const count = Math.max(0, state.count - 1)
      return { count, isLoading: count > 0 }
    }),
}))
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/store/useLoadingStore.test.ts 2>&1 | tail -5
```

Expected: PASS — 5 tests passing.

- [ ] **Step 5: Write failing tests for `useAuthStore`**

Create `src/__tests__/store/useAuthStore.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { act } from '@testing-library/react'
import { useAuthStore } from '@/store/useAuthStore'

const mockUser = { id: 1, name: 'Alice', email: 'alice@example.com', roles: ['employee'] }

beforeEach(() => {
  useAuthStore.setState({ user: null })
  sessionStorage.clear()
})

describe('useAuthStore', () => {
  it('starts with null user', () => {
    expect(useAuthStore.getState().user).toBeNull()
  })

  it('setUser stores user info', () => {
    act(() => useAuthStore.getState().setUser(mockUser))
    expect(useAuthStore.getState().user).toEqual(mockUser)
  })

  it('clearUser resets to null', () => {
    act(() => {
      useAuthStore.getState().setUser(mockUser)
      useAuthStore.getState().clearUser()
    })
    expect(useAuthStore.getState().user).toBeNull()
  })

  it('user is persisted to sessionStorage after setUser', () => {
    act(() => useAuthStore.getState().setUser(mockUser))
    const stored = JSON.parse(sessionStorage.getItem('auth-store') ?? '{}')
    expect(stored.state?.user?.email).toBe('alice@example.com')
  })
})
```

- [ ] **Step 6: Run tests to confirm they fail**

```bash
pnpm test src/__tests__/store/useAuthStore.test.ts 2>&1 | tail -5
```

Expected: FAIL — "Cannot find module '@/store/useAuthStore'"

- [ ] **Step 7: Implement `useAuthStore`**

Create `src/store/useAuthStore.ts`:
```ts
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

export interface AuthUser {
  id: number
  name: string
  email: string
  roles: string[]
}

interface AuthState {
  user: AuthUser | null
  setUser: (user: AuthUser) => void
  clearUser: () => void
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      setUser: (user) => set({ user }),
      clearUser: () => set({ user: null }),
    }),
    {
      name: 'auth-store',
      storage: {
        getItem: (name) => {
          const value = sessionStorage.getItem(name)
          return value ? JSON.parse(value) : null
        },
        setItem: (name, value) => sessionStorage.setItem(name, JSON.stringify(value)),
        removeItem: (name) => sessionStorage.removeItem(name),
      },
    },
  ),
)
```

- [ ] **Step 8: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/store/useAuthStore.test.ts 2>&1 | tail -5
```

Expected: PASS — 4 tests passing.

- [ ] **Step 9: Commit**

```bash
git add src/store/ src/__tests__/store/
git commit -m "feat(TASK-AUTH-0006): add useLoadingStore and useAuthStore with tests"
```

---

## Task 3: Axios Types + Base Instance

**Files:**
- Create: `src/lib/http/types.ts`
- Create: `src/lib/http/instance.ts`

- [ ] **Step 1: Create shared types**

Create `src/lib/http/types.ts`:
```ts
import type { AxiosRequestConfig } from 'axios'

export interface ApiResponse<T = unknown> {
  code: string
  message: string
  data: T
  traceId?: string
}

// Extend AxiosRequestConfig to support per-request silent flag
declare module 'axios' {
  interface AxiosRequestConfig {
    silent?: boolean
  }
}

export type { AxiosRequestConfig }
```

- [ ] **Step 2: Create Axios instance**

Create `src/lib/http/instance.ts`:
```ts
import axios from 'axios'

if (!process.env.NEXT_PUBLIC_BFF_URL) {
  console.warn('[http] NEXT_PUBLIC_BFF_URL is not set — all API calls will fail')
}

export const axiosInstance = axios.create({
  baseURL: process.env.NEXT_PUBLIC_BFF_URL,
  timeout: 10_000,
  withCredentials: true,
})
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/
git commit -m "feat(TASK-AUTH-0006): add axios base instance and shared API types"
```

---

## Task 4: Loading Interceptor

**Files:**
- Create: `src/lib/http/interceptors/loading.ts`
- Create: `src/__tests__/lib/http/interceptors/loading.test.ts`

- [ ] **Step 1: Write failing tests**

Create `src/__tests__/lib/http/interceptors/loading.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest'
import axios from 'axios'
import MockAdapter from 'axios-mock-adapter'
import { useLoadingStore } from '@/store/useLoadingStore'
import { applyLoadingInterceptor } from '@/lib/http/interceptors/loading'

beforeEach(() => {
  useLoadingStore.setState({ count: 0, isLoading: false })
})

describe('applyLoadingInterceptor', () => {
  it('increments count on request and decrements on response', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyLoadingInterceptor(instance)
    mock.onGet('/test').reply(200, { code: 'OK', message: 'ok', data: null })

    const req = instance.get('/test')
    expect(useLoadingStore.getState().count).toBe(1)
    await req
    expect(useLoadingStore.getState().count).toBe(0)
  })

  it('does not increment count when silent: true', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyLoadingInterceptor(instance)
    mock.onGet('/silent').reply(200, { code: 'OK', message: 'ok', data: null })

    await instance.get('/silent', { silent: true })
    expect(useLoadingStore.getState().count).toBe(0)
  })

  it('decrements count on request error', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyLoadingInterceptor(instance)
    mock.onGet('/fail').reply(500)

    try { await instance.get('/fail') } catch {}
    expect(useLoadingStore.getState().count).toBe(0)
  })

  it('never lets count go below 0', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyLoadingInterceptor(instance)
    mock.onGet('/test').reply(200, { code: 'OK', message: 'ok', data: null })

    // Force extra decrement
    useLoadingStore.getState().decrement()
    await instance.get('/test')
    expect(useLoadingStore.getState().count).toBe(0)
  })
})
```

- [ ] **Step 2: Run to confirm failure**

```bash
pnpm test src/__tests__/lib/http/interceptors/loading.test.ts 2>&1 | tail -5
```

Expected: FAIL — "Cannot find module"

- [ ] **Step 3: Implement loading interceptor**

Create `src/lib/http/interceptors/loading.ts`:
```ts
import type { AxiosInstance } from 'axios'
import { useLoadingStore } from '@/store/useLoadingStore'

export function applyLoadingInterceptor(instance: AxiosInstance): void {
  instance.interceptors.request.use((config) => {
    if (!config.silent) {
      useLoadingStore.getState().increment()
    }
    return config
  })

  const decrement = (config: { silent?: boolean }) => {
    if (!config?.silent) {
      useLoadingStore.getState().decrement()
    }
  }

  instance.interceptors.response.use(
    (response) => {
      decrement(response.config)
      return response
    },
    (error) => {
      decrement(error.config)
      return Promise.reject(error)
    },
  )
}
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/lib/http/interceptors/loading.test.ts 2>&1 | tail -5
```

Expected: PASS — 4 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/lib/http/interceptors/loading.ts src/__tests__/lib/http/interceptors/loading.test.ts
git commit -m "feat(TASK-AUTH-0006): loading interceptor with counter and silent opt-out"
```

---

## Task 5: Error Interceptor (Envelope Unwrap + Toast)

**Files:**
- Create: `src/lib/http/interceptors/error.ts`
- Create: `src/__tests__/lib/http/interceptors/error.test.ts`

- [ ] **Step 1: Write failing tests**

Create `src/__tests__/lib/http/interceptors/error.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import axios from 'axios'
import MockAdapter from 'axios-mock-adapter'
import { applyErrorInterceptor } from '@/lib/http/interceptors/error'

// Mock sonner toast
vi.mock('sonner', () => ({
  toast: { error: vi.fn() },
}))

import { toast } from 'sonner'

beforeEach(() => {
  vi.clearAllMocks()
})

describe('applyErrorInterceptor', () => {
  it('returns data.data when code is OK', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyErrorInterceptor(instance)
    mock.onGet('/ok').reply(200, { code: 'OK', message: 'success', data: { id: 1 } })

    const result = await instance.get('/ok')
    expect(result).toEqual({ id: 1 })
    expect(toast.error).not.toHaveBeenCalled()
  })

  it('shows toast and rejects when code is not OK', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyErrorInterceptor(instance)
    mock.onGet('/err').reply(200, {
      code: 'bff-2001',
      message: '用户不存在',
      data: null,
      traceId: 'abc-123',
    })

    await expect(instance.get('/err')).rejects.toMatchObject({ code: 'bff-2001' })
    expect(toast.error).toHaveBeenCalledWith('用户不存在')
  })

  it('logs traceId to console on business error', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyErrorInterceptor(instance)
    const consoleSpy = vi.spyOn(console, 'error').mockImplementation(() => {})
    mock.onGet('/trace').reply(200, {
      code: 'bff-2001',
      message: 'error',
      data: null,
      traceId: 'trace-xyz',
    })

    await expect(instance.get('/trace')).rejects.toBeDefined()
    expect(consoleSpy).toHaveBeenCalledWith('[traceId]', 'trace-xyz')
    consoleSpy.mockRestore()
  })

  it('shows generic toast on HTTP network error (non-401)', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyErrorInterceptor(instance)
    mock.onGet('/network').reply(500)

    await expect(instance.get('/network')).rejects.toBeDefined()
    expect(toast.error).toHaveBeenCalledWith('网络异常，请稍后重试')
  })

  it('does NOT show generic toast on 401 (handled by auth interceptor)', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyErrorInterceptor(instance)
    mock.onGet('/auth').reply(401)

    await expect(instance.get('/auth')).rejects.toBeDefined()
    expect(toast.error).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Run to confirm failure**

```bash
pnpm test src/__tests__/lib/http/interceptors/error.test.ts 2>&1 | tail -5
```

Expected: FAIL

- [ ] **Step 3: Implement error interceptor**

Create `src/lib/http/interceptors/error.ts`:
```ts
import type { AxiosInstance } from 'axios'
import { toast } from 'sonner'
import type { ApiResponse } from '@/lib/http/types'

export function applyErrorInterceptor(instance: AxiosInstance): void {
  instance.interceptors.response.use(
    (response) => {
      const body = response.data as ApiResponse
      if (body.code !== 'OK') {
        toast.error(body.message)
        if (body.traceId) {
          console.error('[traceId]', body.traceId)
        }
        return Promise.reject(body)
      }
      // Unwrap envelope — callers receive data.data directly
      return body.data
    },
    (error) => {
      // 401 is handled by auth interceptor — skip generic toast
      if (error.response?.status === 401) {
        return Promise.reject(error)
      }
      toast.error('网络异常，请稍后重试')
      return Promise.reject(error)
    },
  )
}
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/lib/http/interceptors/error.test.ts 2>&1 | tail -5
```

Expected: PASS — 5 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/lib/http/interceptors/error.ts src/__tests__/lib/http/interceptors/error.test.ts
git commit -m "feat(TASK-AUTH-0006): error interceptor — envelope unwrap, toast, traceId"
```

---

## Task 6: Auth Interceptor (401 Queue + Refresh)

**Files:**
- Create: `src/lib/http/interceptors/auth.ts`
- Create: `src/__tests__/lib/http/interceptors/auth.test.ts`

- [ ] **Step 1: Write failing tests**

Create `src/__tests__/lib/http/interceptors/auth.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import axios from 'axios'
import MockAdapter from 'axios-mock-adapter'
import { useAuthStore } from '@/store/useAuthStore'
import { applyAuthInterceptor } from '@/lib/http/interceptors/auth'

// Mock next/navigation
const mockPush = vi.fn()
vi.mock('next/navigation', () => ({ useRouter: () => ({ push: mockPush }) }))

// Mock the router push used directly (not via hook) inside the interceptor
vi.mock('next/navigation', () => ({
  redirect: vi.fn(),
}))

const mockUser = { id: 1, name: 'Alice', email: 'alice@example.com', roles: ['employee'] }

beforeEach(() => {
  vi.clearAllMocks()
  useAuthStore.setState({ user: mockUser })
})

describe('applyAuthInterceptor', () => {
  it('retries original request after successful refresh', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyAuthInterceptor(instance)

    mock.onGet('/protected').replyOnce(401).onGet('/protected').reply(200, { code: 'OK', data: { ok: true } })
    mock.onPost('/auth/refresh').reply(200, { code: 'OK', data: null })

    const result = await instance.get('/protected')
    expect(result.data).toEqual({ code: 'OK', data: { ok: true } })
  })

  it('only calls /auth/refresh once for concurrent 401s', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    applyAuthInterceptor(instance)

    mock.onGet('/a').replyOnce(401).onGet('/a').reply(200, { code: 'OK', data: 'a' })
    mock.onGet('/b').replyOnce(401).onGet('/b').reply(200, { code: 'OK', data: 'b' })
    mock.onPost('/auth/refresh').replyOnce(200, { code: 'OK', data: null })

    await Promise.all([instance.get('/a'), instance.get('/b')])

    const refreshCalls = mock.history.post.filter((r) => r.url === '/auth/refresh')
    expect(refreshCalls).toHaveLength(1)
  })

  it('clears user and redirects to /login when refresh fails', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    const { redirect } = await import('next/navigation')
    applyAuthInterceptor(instance)

    mock.onGet('/protected').reply(401)
    mock.onPost('/auth/refresh').reply(401)

    await expect(instance.get('/protected')).rejects.toBeDefined()
    expect(useAuthStore.getState().user).toBeNull()
    expect(redirect).toHaveBeenCalledWith('/login')
  })

  it('does not loop when /auth/refresh itself returns 401', async () => {
    const instance = axios.create()
    const mock = new MockAdapter(instance)
    const { redirect } = await import('next/navigation')
    applyAuthInterceptor(instance)

    mock.onPost('/auth/refresh').reply(401)

    await expect(instance.post('/auth/refresh')).rejects.toBeDefined()
    // refresh endpoint 401 must NOT trigger another refresh attempt
    const refreshCalls = mock.history.post.filter((r) => r.url === '/auth/refresh')
    expect(refreshCalls).toHaveLength(1)
    expect(redirect).toHaveBeenCalledWith('/login')
  })
})
```

- [ ] **Step 2: Run to confirm failure**

```bash
pnpm test src/__tests__/lib/http/interceptors/auth.test.ts 2>&1 | tail -5
```

Expected: FAIL

- [ ] **Step 3: Implement auth interceptor**

Create `src/lib/http/interceptors/auth.ts`:
```ts
import type { AxiosInstance, InternalAxiosRequestConfig } from 'axios'
import { redirect } from 'next/navigation'
import { useAuthStore } from '@/store/useAuthStore'

let isRefreshing = false
let queue: Array<{
  resolve: (value: unknown) => void
  reject: (reason: unknown) => void
  config: InternalAxiosRequestConfig
}> = []

function flushQueue(instance: AxiosInstance, error?: unknown) {
  queue.forEach(({ resolve, reject, config }) => {
    if (error) {
      reject(error)
    } else {
      resolve(instance(config))
    }
  })
  queue = []
}

function clearAndRedirect() {
  useAuthStore.getState().clearUser()
  redirect('/login')
}

export function applyAuthInterceptor(instance: AxiosInstance): void {
  instance.interceptors.response.use(undefined, async (error) => {
    if (error.response?.status !== 401) {
      return Promise.reject(error)
    }

    const originalConfig: InternalAxiosRequestConfig = error.config

    // Infinite-loop guard: if the refresh endpoint itself 401s, bail out
    if (originalConfig.url === '/auth/refresh') {
      clearAndRedirect()
      return Promise.reject(error)
    }

    if (isRefreshing) {
      // Queue this request — it will be replayed after refresh completes
      return new Promise((resolve, reject) => {
        queue.push({ resolve, reject, config: originalConfig })
      })
    }

    isRefreshing = true

    try {
      await instance.post('/auth/refresh', {}, { silent: true })
      flushQueue(instance)
      return instance(originalConfig)
    } catch (refreshError) {
      flushQueue(instance, refreshError)
      clearAndRedirect()
      return Promise.reject(refreshError)
    } finally {
      isRefreshing = false
    }
  })
}
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/lib/http/interceptors/auth.test.ts 2>&1 | tail -5
```

Expected: PASS — 4 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/lib/http/interceptors/auth.ts src/__tests__/lib/http/interceptors/auth.test.ts
git commit -m "feat(TASK-AUTH-0006): auth interceptor — 401 queue, refresh, replay, loop guard"
```

---

## Task 7: Compose HTTP Singleton + Logout Helper

**Files:**
- Create: `src/lib/http/index.ts`
- Create: `src/lib/auth/logout.ts`
- Create: `src/__tests__/lib/auth/logout.test.ts`

- [ ] **Step 1: Create `src/lib/http/index.ts`**

```ts
import { axiosInstance } from './instance'
import { applyLoadingInterceptor } from './interceptors/loading'
import { applyAuthInterceptor } from './interceptors/auth'
import { applyErrorInterceptor } from './interceptors/error'

// Order matters: loading → auth → error
applyLoadingInterceptor(axiosInstance)
applyAuthInterceptor(axiosInstance)
applyErrorInterceptor(axiosInstance)

export const http = axiosInstance
export type { ApiResponse } from './types'
```

- [ ] **Step 2: Write failing tests for `logout`**

Create `src/__tests__/lib/auth/logout.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { useAuthStore } from '@/store/useAuthStore'

const mockPush = vi.fn()
vi.mock('next/navigation', () => ({ redirect: vi.fn() }))
vi.mock('@/lib/http', () => ({
  http: { post: vi.fn() },
}))

import { logout } from '@/lib/auth/logout'
import { redirect } from 'next/navigation'
import { http } from '@/lib/http'

const mockUser = { id: 1, name: 'Alice', email: 'alice@example.com', roles: ['employee'] }

beforeEach(() => {
  vi.clearAllMocks()
  useAuthStore.setState({ user: mockUser })
})

describe('logout', () => {
  it('calls POST /auth/logout, clears user, and redirects to /login', async () => {
    vi.mocked(http.post).mockResolvedValueOnce(undefined)
    await logout()
    expect(http.post).toHaveBeenCalledWith('/auth/logout', {}, { silent: true })
    expect(useAuthStore.getState().user).toBeNull()
    expect(redirect).toHaveBeenCalledWith('/login')
  })

  it('still clears user and redirects even if API call fails (fail-safe)', async () => {
    vi.mocked(http.post).mockRejectedValueOnce(new Error('network error'))
    await logout()
    expect(useAuthStore.getState().user).toBeNull()
    expect(redirect).toHaveBeenCalledWith('/login')
  })
})
```

- [ ] **Step 3: Run to confirm failure**

```bash
pnpm test src/__tests__/lib/auth/logout.test.ts 2>&1 | tail -5
```

Expected: FAIL

- [ ] **Step 4: Implement `logout`**

Create `src/lib/auth/logout.ts`:
```ts
import { redirect } from 'next/navigation'
import { http } from '@/lib/http'
import { useAuthStore } from '@/store/useAuthStore'

export async function logout(): Promise<void> {
  try {
    await http.post('/auth/logout', {}, { silent: true })
  } catch {
    // Fail-safe: even if API errors, still clear local state
  } finally {
    useAuthStore.getState().clearUser()
    redirect('/login')
  }
}
```

- [ ] **Step 5: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/lib/auth/logout.test.ts 2>&1 | tail -5
```

Expected: PASS — 2 tests passing.

- [ ] **Step 6: Commit**

```bash
git add src/lib/http/index.ts src/lib/auth/ src/__tests__/lib/auth/
git commit -m "feat(TASK-AUTH-0006): compose http singleton, add logout helper with fail-safe"
```

---

## Task 8: LoadingOverlay + AppProviders + Layout

**Files:**
- Create: `src/components/ui/LoadingOverlay.tsx`
- Create: `src/components/providers/AppProviders.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Create `LoadingOverlay`**

Create `src/components/ui/LoadingOverlay.tsx`:
```tsx
'use client'

import { useLoadingStore } from '@/store/useLoadingStore'

export function LoadingOverlay() {
  const isLoading = useLoadingStore((state) => state.isLoading)

  if (!isLoading) return null

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50">
      <div className="h-12 w-12 animate-spin rounded-full border-4 border-white/20 border-t-white" />
    </div>
  )
}
```

- [ ] **Step 2: Create `AppProviders`**

Create `src/components/providers/AppProviders.tsx`:
```tsx
'use client'

import { Toaster } from 'sonner'
import { LoadingOverlay } from '@/components/ui/LoadingOverlay'

export function AppProviders({ children }: { children: React.ReactNode }) {
  return (
    <>
      {children}
      <LoadingOverlay />
      <Toaster position="top-right" richColors closeButton />
    </>
  )
}
```

- [ ] **Step 3: Update `src/app/layout.tsx`**

```tsx
import type { Metadata } from 'next'
import { AppProviders } from '@/components/providers/AppProviders'
import './globals.css'

export const metadata: Metadata = {
  title: '积分商城 | 员工管理系统',
  description: 'Employee Points Mall',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="zh-CN">
      <body>
        <AppProviders>{children}</AppProviders>
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Verify build still passes**

```bash
pnpm build 2>&1 | tail -10
```

Expected: Build success.

- [ ] **Step 5: Commit**

```bash
git add src/components/ src/app/layout.tsx
git commit -m "feat(TASK-AUTH-0006): LoadingOverlay, AppProviders, update layout"
```

---

## Task 9: Login Page

**Files:**
- Create: `src/app/(auth)/login/page.tsx`

- [ ] **Step 1: Create login page**

Create `src/app/(auth)/login/page.tsx`:
```tsx
'use client'

import { useEffect } from 'react'
import { useRouter } from 'next/navigation'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { http } from '@/lib/http'
import { useAuthStore, type AuthUser } from '@/store/useAuthStore'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'

const loginSchema = z.object({
  email: z.string().email('请输入有效邮箱'),
  password: z.string().min(6, '密码至少 6 位'),
})

type LoginForm = z.infer<typeof loginSchema>

export default function LoginPage() {
  const router = useRouter()
  const { user, setUser } = useAuthStore()

  // Already authenticated — redirect away
  useEffect(() => {
    if (user) router.push('/dashboard')
  }, [user, router])

  const {
    register,
    handleSubmit,
    formState: { errors, isSubmitting },
  } = useForm<LoginForm>({ resolver: zodResolver(loginSchema) })

  const onSubmit = async (data: LoginForm) => {
    const result = await http.post<{ user: AuthUser }>('/auth/login', data)
    setUser(result.user)
    router.push('/dashboard')
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-gradient-to-br from-slate-950 to-indigo-950">
      <div className="w-full max-w-sm rounded-2xl border border-white/20 bg-white/10 p-8 shadow-2xl backdrop-blur-md">
        {/* Logo + title */}
        <div className="mb-8 text-center">
          <div className="mx-auto mb-3 flex h-12 w-12 items-center justify-center rounded-xl bg-gradient-to-br from-indigo-500 to-purple-600 text-white text-2xl shadow-lg">
            🏪
          </div>
          <h1 className="text-xl font-semibold text-white">积分商城</h1>
          <p className="mt-1 text-sm text-white/60">员工管理系统</p>
        </div>

        {/* Form */}
        <form onSubmit={handleSubmit(onSubmit)} noValidate className="space-y-5">
          <div className="space-y-1.5">
            <Label htmlFor="email" className="text-white/80">
              邮箱
            </Label>
            <Input
              id="email"
              type="email"
              placeholder="you@company.com"
              autoComplete="email"
              className="border-white/20 bg-white/10 text-white placeholder:text-white/40 focus:border-indigo-400"
              {...register('email')}
            />
            {errors.email && (
              <p className="text-xs text-red-400">{errors.email.message}</p>
            )}
          </div>

          <div className="space-y-1.5">
            <Label htmlFor="password" className="text-white/80">
              密码
            </Label>
            <Input
              id="password"
              type="password"
              placeholder="••••••••"
              autoComplete="current-password"
              className="border-white/20 bg-white/10 text-white placeholder:text-white/40 focus:border-indigo-400"
              {...register('password')}
            />
            {errors.password && (
              <p className="text-xs text-red-400">{errors.password.message}</p>
            )}
          </div>

          <Button
            type="submit"
            disabled={isSubmitting}
            className="w-full bg-gradient-to-r from-indigo-500 to-purple-600 text-white hover:from-indigo-600 hover:to-purple-700"
          >
            {isSubmitting ? '登录中...' : '登 录'}
          </Button>
        </form>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verify the page renders without error**

```bash
pnpm dev &
sleep 5
curl -s http://localhost:3003/login | grep -c "积分商城"
kill %1
```

Expected: output is `1` (page renders with title).

- [ ] **Step 3: Commit**

```bash
git add src/app/\(auth\)/
git commit -m "feat(TASK-AUTH-0006): login page — dark gradient UI, RHF + Zod validation"
```

---

## Task 10: `middleware.ts` Route Guard

**Files:**
- Create: `src/middleware.ts`
- Create: `src/__tests__/middleware.test.ts`

- [ ] **Step 1: Write failing tests**

Create `src/__tests__/middleware.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { NextRequest, NextResponse } from 'next/server'
import { middleware, config } from '@/middleware'

function makeRequest(path: string, hasCookie = false): NextRequest {
  const req = new NextRequest(`http://localhost:3003${path}`)
  if (hasCookie) {
    req.cookies.set('access_token', 'mock-jwt-value')
  }
  return req
}

describe('middleware', () => {
  it('allows access to /login without cookie', async () => {
    const res = await middleware(makeRequest('/login', false))
    expect(res.status).not.toBe(307)
  })

  it('allows access to /auth/refresh without cookie', async () => {
    const res = await middleware(makeRequest('/auth/refresh', false))
    expect(res.status).not.toBe(307)
  })

  it('redirects unauthenticated user from /dashboard to /login', async () => {
    const res = await middleware(makeRequest('/dashboard', false))
    expect(res.status).toBe(307)
    expect(res.headers.get('location')).toContain('/login')
  })

  it('allows authenticated user to access /dashboard', async () => {
    const res = await middleware(makeRequest('/dashboard', true))
    expect(res).toBeUndefined() // NextResponse.next() returns undefined in test context
  })

  it('config matcher excludes _next and static files', () => {
    expect(config.matcher).toContain('/((?!_next|favicon.ico|.*\\..*).*)') 
  })
})
```

- [ ] **Step 2: Run to confirm failure**

```bash
pnpm test src/__tests__/middleware.test.ts 2>&1 | tail -5
```

Expected: FAIL

- [ ] **Step 3: Implement `middleware.ts`**

Create `src/middleware.ts`:
```ts
import { NextRequest, NextResponse } from 'next/server'

const PUBLIC_ROUTES = ['/login', '/auth/refresh', '/auth/logout']

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // Allow public routes through
  if (PUBLIC_ROUTES.some((route) => pathname.startsWith(route))) {
    return NextResponse.next()
  }

  // Check for access_token cookie (existence only — Edge Runtime cannot verify JWT)
  const token = request.cookies.get('access_token')
  if (!token) {
    const loginUrl = new URL('/login', request.url)
    return NextResponse.redirect(loginUrl)
  }

  return NextResponse.next()
}

export const config = {
  matcher: ['/((?!_next|favicon.ico|.*\\..*).*)'],
}
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
pnpm test src/__tests__/middleware.test.ts 2>&1 | tail -5
```

Expected: PASS — 5 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/middleware.ts src/__tests__/middleware.test.ts
git commit -m "feat(TASK-AUTH-0006): middleware route guard — cookie check, public route whitelist"
```

---

## Task 11: Update Task File + Run All Tests + Final Commit

- [ ] **Step 1: Run all tests**

```bash
cd points-mall-frontend
pnpm test 2>&1 | tail -20
```

Expected: All tests pass. No failures.

- [ ] **Step 2: Run pnpm tasks:sync**

```bash
cd /path/to/fullstack-points-mall
pnpm run tasks:sync
```

Expected: `.tasks/_index.md` updated.

- [ ] **Step 3: Update TASK-AUTH-0006.md**

In `.tasks/auth/TASK-AUTH-0006.md`:
- Set `status: in-dev`
- Fill in `code_files` list (all new/modified files from the file map above)
- Append to Status Change History:
  ```
  | 2026-06-26 | spec-ready | in-dev | AI | Implementation started |
  ```

- [ ] **Step 4: Final commit**

```bash
git add .tasks/auth/TASK-AUTH-0006.md .tasks/_index.md
git commit -m "chore(TASK-AUTH-0006): status spec-ready → in-dev, fill code_files"
```

---

## Self-Review Checklist

- [x] **AC-01** Loading overlay shows on all non-silent requests → Task 4 loading interceptor
- [x] **AC-02** `silent: true` skips overlay → Task 4 loading interceptor
- [x] **AC-03** Business error shows toast → Task 5 error interceptor
- [x] **AC-04** Network error shows generic toast → Task 5 error interceptor
- [x] **AC-05** Envelope unwrapped → Task 5 error interceptor (returns `body.data`)
- [x] **AC-06** traceId logged to console → Task 5 error interceptor
- [x] **AC-07** NEXT_PUBLIC_BFF_URL documented in .env.example → Task 1
- [x] **AC-08** Invalid email shows inline error → Task 9 login page (Zod + RHF)
- [x] **AC-09** Short password shows inline error → Task 9 login page
- [x] **AC-10** Successful login → setUser + redirect → Task 9 login page
- [x] **AC-11** Failed login → toast automatic → Task 5 error interceptor (zero per-page setup)
- [x] **AC-12** Already-authed → redirect from /login → Task 9 login page useEffect
- [x] **AC-13** SessionStorage persistence → Task 2 useAuthStore persist
- [x] **AC-14** 401 retried after refresh → Task 6 auth interceptor
- [x] **AC-15** Concurrent 401s → single refresh call → Task 6 auth interceptor (`isRefreshing` flag)
- [x] **AC-16** All queued requests replayed → Task 6 auth interceptor (`flushQueue`)
- [x] **AC-17** Refresh failure → clearUser + redirect → Task 6 auth interceptor
- [x] **AC-18** Infinite loop guard → Task 6 auth interceptor (url check)
- [x] **AC-19** middleware route guard → Task 10
- [x] **AC-20** Error path decrements loading counter → Task 4 (error handler in loading interceptor)
- [x] **AC-21** Counter clamps to 0 → Task 2 useLoadingStore (`Math.max(0, ...)`)
- [x] **AC-22** /auth/refresh called with `silent: true` → Task 6 auth interceptor
- [x] **AC-23** Logout fail-safe → Task 7 logout helper (finally block)
